# 工程结构

- `WeBoxApp`：SwiftUI 应用界面。
- `Core`：微信检测、复制、签名和进程服务。
- `Core/InstanceHealthChecker`：副本路径、Bundle Identifier、签名和原版微信版本的统一检查。
- `Models`：领域数据模型。
- `Database`：SQLite 持久化。
- `Tests`：自动测试。
- `Docs`：使用、测试与发布说明。
